import { Config } from './../../helpers/config';

const MODULE_CONFIG = new Config({
  NAME: 'default',
  TYPE: 'page',
  // PAGE_STATE: '',
  // PAGE_URL: '',
  PAGE_LOADER: true
});

export default MODULE_CONFIG;
